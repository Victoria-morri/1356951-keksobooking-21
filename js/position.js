'use strict';

(function () {
  const PIN_WIDTH_HALF = 25;
  const PIN_HEIGHT = 70;
  const MAX_WIDTH = 1200;
  const MAX_HEIGHT = 630;

  window.position = {
    PIN_WIDTH_HALF,
    PIN_HEIGHT,
    MAX_WIDTH,
    MAX_HEIGHT
  };

}());
